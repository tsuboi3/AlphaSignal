# AlphaSignal プロジェクト AI運用規則 (Workspace Rules)

> 本ファイルは Antigravity / Gemini CLI 起動時に自動適用されるワークスペース共通ルールです。

## ⚠️ 必須規則：AlphaSignal.md の自動読み込みと更新

当プロジェクトにおける唯一の信頼できる情報源（Single Source of Truth）は **`AlphaSignal.md`** です。
AIエージェントは以下のライフサイクル規則を厳守すること：

1. **作業開始時 / セッション再開時**:
   - ユーザーからの最初のタスク着手時に、必ず `AlphaSignal.md` を読み込むこと。
   - 「現在のバージョン」「ディレクトリ構成」「アーキテクチャ」「変更履歴」を確認し、前提知識を最新化してから作業すること。

2. **作業終了時 / タスク完了時**:
   - ファイルの追加・修正、機能実装、または設定変更を行った場合は、必ず `AlphaSignal.md` の「7. 変更履歴」セクションにバージョン・日付・作業内容を追記・更新すること。

3. **アーキテクチャ・整合性の遵守**:
   - 変更にあたっては `AlphaSignal.md` に定義された設計原則（PyTorch版Transformer、SQLite永続化、Google Drive同期、日本株100銘柄等）に従い、互換性を維持すること。
