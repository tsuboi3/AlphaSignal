# AlphaSignal トレード活用・全体システム統合計画書

## 1. 概要
本計画書は、AlphaSignal システムに含まれるすべてのコンポーネント（データ取得・前処理・永続化、機械学習/ディープラーニング予測モデル、ニュースセンチメント分析、アルファ抽出・シグナル結合エンジン）を集約し、実トレードに活用するための統合CUIメニューおよび全自動パイプラインの構成設計を定義するものである。

---

## 2. 全体システム構成アーキテクチャ

システム全体のデータフローとコンポーネント関係は以下の通り：

```
                              ┌────────────────────────┐
                              │  ユーザー (対話式CUI)   │
                              └───────────┬────────────┘
                                          │
                                          ▼
                              ┌────────────────────────┐
                              │   main.py / src/menu.py │
                              └───────────┬────────────┘
                                          │
      ┌───────────────────────────────────┼───────────────────────────────────┐
      ▼                                   ▼                                   ▼
【1. 株価・ニュースデータ層】        【2. アルファ抽出・シグナル結合層】     【3. 機械学習アンサンブル層】
 ┌────────────────────────┐          ┌────────────────────────┐         ┌────────────────────────┐
 │ - yfinance             │          │ - AlphaEngine          │         │ - LightGBM             │
 │ - News Sentiment (NIS) │          │   (Ridge正則化・L1ノルム) │         │ - PyTorch Transformer  │
 │ - SQLite (database.py) │          │ - AlphaCombiner        │         │ - Stacking Meta-Model  │
 └───────────┬────────────┘          │   (直交化・残差アルファ) │         └───────────┬────────────┘
             │                       └───────────┬────────────┘                     │
             │                                   │                                  │
             └───────────────────────────────────┼───────────────────────────────────┘
                                                 │
                                                 ▼
                              ┌────────────────────────────────┐
                              │ 4. 統合トレードシグナル判定    │
                              │    - 売買推奨 (Buy/Sell/Hold)  │
                              │    - バックテスト指標表示      │
                              │    - 結果出力 (JSON, CSV)      │
                              └────────────────────────────────┘
```

---

## 3. 各モジュールと統合仕様

### 3.1 データ取得・永続化層
- **株価データ (`src/features.py`)**: `yfinance` 経由でOHLCVデータを取得し、SQLite DB (`alphasignal.db`) に重複なく永続化。
- **ニュースデータ (`src/news_sentiment.py`)**: ニュースセンチメントスコアから指数関数的減衰を適用したニュースインパクト指数 (NIS) を計算。
- **データベース (`src/database.py`)**: データの一貫性を保ち、過去データの検索・蓄積を行う。

### 3.2 機械学習/ディープラーニング予測層
- **LightGBM (`src/lgbm_model.py`)**: テクニカル指標・価格特徴量・NIS を用いた勾配ブースティング学習。
- **Transformer (`src/transformer_model.py`)**: PyTorch 実装の Multi-Head Attention シーケンス予測モデル。
- **スタッキングアンサンブル (`src/ensemble.py`)**: LightGBM 予測、Transformer 予測、NIS を説明変数とする線形回帰メタモデルで総合予測を実施。

### 3.3 アルファ抽出・シグナル結合層
- **AlphaEngine (`src/alpha_engine.py`)**: 過去リターン行列からRidge正則化とL1正規化により各シグナルの最適結合ウェイトを算出。
- **AlphaCombiner (`src/signals/alpha_combiner.py`)**: 因子直交化 (`FactorOrthogonalizer`) を用いて共線性を排除し、純粋な残差アルファ (Residual Alpha) を抽出してウェイト結合。

---

## 4. 対話式CUIメニュー UI 設計 (`src/menu.py` & `main.py`)

CUIメニューにおいて、トレード活用に特化した以下の主要機能オプションを提供する：

1. **株価予測・アンサンブル実行 (LightGBM + Transformer + NIS)**
   - 任意銘柄（日英対応）・期間での価格予測とモデル別性能評価。
2. **アルファ・シグナル合成実行 (AlphaEngine / AlphaCombiner)**
   - 複数アルファシグナルの結合ウェイト算出および合成シグナル値の試算。
3. **総合シグナル診断・売買判定 (Trade Signal Analysis)**
   - 予測価格、リターン予測値、過去アルファシグナルを統合し、実トレード向けの売買指示（BUY / SELL / HOLD）とバックテスト実績を表示。
4. **データベースステータス確認 (Database Inspection)**
   - `alphasignal.db` の保持レコード数、データ欠損状態、最終更新日などの状態確認。
5. **終了**

---

## 5. 実行環境と移行方針
- `main.py` を本システムの統一メインエントリとし、コマンドライン引数実行（非対話CLI）と対話式メニュー実行の両方をシームレスにサポートする。
- 既存の `alphasignal.py` から `main.py` への主要機能移設と統合を行い、完全互換性を維持する。
